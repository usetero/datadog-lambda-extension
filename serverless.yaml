# Copyright 2024 Datadog, Inc.
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# This file is derived from:
# https://github.com/DataDog/serverless-remote-instrumentation/blob/prod/template.yaml
#
# Modifications made by Tero.

AWSTemplateFormatVersion: "2010-09-09"

Description: Tero Lambda Remote Instrumentation App

Mappings:
  Constants:
    DdRemoteInstrumentApp:
      Version: 2.1.0
    DdRemoteInstrumentLayerAwsAccount:
      Number: 464622532012
    InstrumenterFunctionName:
      Name: tero-remote-instrumenter
    DdRemoteInstrumentLayerVersion:
      # Find latest version at: https://github.com/DataDog/serverless-remote-instrumentation/releases
      # Or in the template: https://datadog-cloudformation-template.s3.amazonaws.com/aws/remote-instrument/latest.yaml
      Version: 17
    DdCIBypassSiteValidation:
      Bypass: false
    TeroExtensionLayerAwsAccount:
      Number: 242046726909
    TeroExtensionLayerUpstream:
      Version: 99
    TeroExtensionLayerVersion:
      Version: 1

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: Remote Instrumenter Configuration
        Parameters:
          - DdApiKey
          - TeroApiKey
          - EnableCodeSigningConfigurations
          - DdInstrumenterFunctionEnv
      - Label:
          default: Additional Configs
        Parameters:
          - DdSite
          - BucketPrefix
          - BucketName
          - UseExistingCloudTrailTrail
          - TrailName

Conditions:
  CreateCodeSigningConfig:
    !Equals [!Ref EnableCodeSigningConfigurations, "true"]
  HasBucketName: !Not [!Equals [!Ref BucketName, ""]]
  CreateCloudTrail: !Not [!Equals [!Ref UseExistingCloudTrailTrail, "true"]]
  UseTeroDevLayer: !Equals [!Ref UseTeroDevExtension, "true"]

Parameters:
  EnableCodeSigningConfigurations:
    Type: String
    AllowedValues:
      - "true"
      - "false"
    Default: "false"
    Description: Whether to enable code signing configurations.
  DdApiKey:
    Type: String
    Description: >-
      The Datadog API key, which can be found from the APIs page
      (/organization-settings/api-keys).
    AllowedPattern: ".+"
    NoEcho: true
  DdSite:
    Type: String
    Default: datadoghq.com
    Description: >-
      The Datadog site where data will be sent. Possible values are `datadoghq.com`, `datadoghq.eu`, `us3.datadoghq.com`, `us5.datadoghq.com`, `ap1.datadoghq.com`, `ap2.datadoghq.com`.
    AllowedPattern: .+
    ConstraintDescription: DdSite is required
  BucketName:
    Type: String
    Default: ""
    Description: >-
      The name of the remote instrumenter bucket. If not provided, the bucket name will be generated based on the BucketPrefix.
  BucketPrefix:
    Type: String
    Default: datadog-remote-instrument
    Description: >-
      A prefix to use for the name of the remote instrumenter bucket. The bucket name will be generated based on this prefix.
  UseExistingCloudTrailTrail:
    Type: String
    AllowedValues:
      - "true"
      - "false"
    Default: "false"
    Description: >-
      Whether to use an existing CloudTrail Trail for the remote instrumenter.  If set to true, there must be a CloudTrail Trail in the
      region where the remote instrumenter is deployed.  Deleting the Trail will cause functions to no longer be instrumented on update.
  TrailName:
    Type: String
    Default: datadog-serverless-instrumentation-trail
    Description: >-
      The name of the CloudTrail Trail to use for the remote instrumenter.  If UseExistingCloudTrailTrail is set to true, this parameter is ignored.
  DdInstrumenterFunctionEnv:
    Type: String
    Default: "dev"
    Description: >-
      The DD_ENV value for the remote instrumenter Lambda function
  TeroApiKey:
    Type: String
    Description: >-
      The Tero API key for policy synchronization.
      Can be found in your Tero dashboard.
    AllowedPattern: ".+"
    NoEcho: true
  UseTeroDevExtension:
    Type: String
    AllowedValues:
      - "true"
      - "false"
    Default: "false"
    Description: >-
      Use the dev version of the Tero extension layer.

Resources:
  CloudFormationLifeCycle:
    Type: "AWS::CloudFormation::CustomResource"
    Version: "1.0"
    Properties:
      ServiceToken: !GetAtt LambdaFunction.Arn
      FunctionName: !FindInMap [Constants, InstrumenterFunctionName, Name]
  LambdaFunction:
    Type: "AWS::Lambda::Function"
    Properties:
      FunctionName: !FindInMap [Constants, InstrumenterFunctionName, Name]
      Architectures:
        - arm64
      Runtime: nodejs20.x
      CodeSigningConfigArn:
        !If [
          CreateCodeSigningConfig,
          !Ref InstrumenterCodeSigningConfig,
          !Ref "AWS::NoValue",
        ]
      ReservedConcurrentExecutions: 1
      Timeout: 900
      MemorySize: 512
      Role: !GetAtt LambdaExecutionRole.Arn
      Handler: /opt/nodejs/node_modules/datadog-remote-instrument/handler.handler
      Layers:
        - !If
          - UseTeroDevLayer
          - !Sub
            - "arn:aws:lambda:${AWS::Region}:${AccountNumber}:layer:Tero-Datadog-Extension-${UpstreamVersion}-ARM-dev:${LayerVersion}"
            - AccountNumber:
                !FindInMap [Constants, TeroExtensionLayerAwsAccount, Number]
              UpstreamVersion:
                !FindInMap [Constants, TeroExtensionLayerUpstream, Version]
              LayerVersion:
                !FindInMap [Constants, TeroExtensionLayerVersion, Version]
          - !Sub
            - "arn:aws:lambda:${AWS::Region}:${AccountNumber}:layer:Tero-Datadog-Extension-${UpstreamVersion}-ARM:${LayerVersion}"
            - AccountNumber:
                !FindInMap [Constants, TeroExtensionLayerAwsAccount, Number]
              UpstreamVersion:
                !FindInMap [Constants, TeroExtensionLayerUpstream, Version]
              LayerVersion:
                !FindInMap [Constants, TeroExtensionLayerVersion, Version]
        - !Join
          - ""
          - - "arn:aws:lambda:"
            - !Ref "AWS::Region"
            - ":"
            - !FindInMap [Constants, DdRemoteInstrumentLayerAwsAccount, Number]
            - ":layer:Datadog-Serverless-Remote-Instrumentation-ARM:"
            - !FindInMap [Constants, DdRemoteInstrumentLayerVersion, Version]
      Environment:
        Variables:
          DD_TRACE_ENABLED: true
          DD_API_KEY: !Ref DdApiKey
          DD_LOG_LEVEL: WARN
          DD_TRACE_DEBUG: false
          DD_SERVICE: Datadog-Instrument
          DD_ENV: !Ref DdInstrumenterFunctionEnv
          DD_SITE: !Ref DdSite
          DATADOG_SITE: !Ref DdSite
          DD_CI_BYPASS_SITE_VALIDATION:
            !FindInMap [Constants, DdCIBypassSiteValidation, Bypass]
          DD_AWS_ACCOUNT_NUMBER: !Sub "${AWS::AccountId}"
          DD_S3_BUCKET: !Ref S3Bucket
          DD_REMOTE_CONFIGURATION_ENABLED: true
          DD_APM_ENABLED: true
          DD_EXTENSION_VERSION: compatibility
          DD_TAGS:
            !Join [
              "",
              [
                "template_version:",
                !FindInMap [Constants, DdRemoteInstrumentApp, Version],
              ],
            ]
          DD_INSTRUMENTER_VERSION:
            !FindInMap [Constants, DdRemoteInstrumentApp, Version]
          DD_POLICY_ENABLED: true
          DD_POLICY_PROVIDERS: !Sub
            - '[{"id":"tero","type":"http","url":"https://sync.usetero.com/v1/policy/sync","headers":[{"name":"Authorization","value":"Bearer ${TeroApiKeyValue}"}],"poll_interval_secs":60}]'
            - TeroApiKeyValue: !Ref TeroApiKey
      Tags:
        - Key: "dd_serverless_service"
          Value: "remote_instrumenter"
      Code:
        ZipFile: |
          const cfnResponse = require("cfn-response");  // file will be auto-injected by CloudFormation
          exports.handler = async (event, context, callback) => {
            return `Succeeded.`;
          };

  LambdaExecutionRole:
    Type: "AWS::IAM::Role"
    Properties:
      Path: "/"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: "sts:AssumeRole"
      Policies:
        - PolicyName: root
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - logs:CreateLogStream
                  - logs:CreateLogGroup
                  - logs:PutLogEvents
                Resource: !Sub arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/lambda/*
              - Effect: Allow
                Action:
                  - lambda:InvokeFunction
                  - lambda:GetFunction
                  - lambda:GetFunctionConfiguration
                  - lambda:UpdateFunctionConfiguration
                  - lambda:TagResource
                  - lambda:UntagResource
                Resource: !Sub arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:*
              - Effect: Allow
                Action:
                  - lambda:GetLayerVersion
                Resource:
                  - !Sub arn:aws:lambda:${AWS::Region}:*:layer:*
              - Effect: Allow
                Action:
                  - lambda:ListTags
                Resource: !Sub arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:*
              - Effect: Allow
                Action:
                  - s3:DeleteObject
                  - s3:ListBucket
                  - s3:GetObject
                  - s3:PutObject
                Resource:
                  - !Join ["", [!GetAtt S3Bucket.Arn, "*"]]
              - Effect: Allow
                Action:
                  - tag:GetResources
                  - tag:TagResources
                  - tag:UntagResources
                  - lambda:ListFunctions
                  - lambda:GetAccountSettings
                Resource: "*"
                Condition:
                  ArnEquals:
                    lambda:SourceFunctionArn: !Sub
                      - arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${InstrumenterFunctionName}
                      - InstrumenterFunctionName:
                          !FindInMap [Constants, InstrumenterFunctionName, Name]

  InstrumenterCodeSigningConfig:
    Type: "AWS::Lambda::CodeSigningConfig"
    Condition: CreateCodeSigningConfig
    Properties:
      Description: "Lambda Code Signing Configuration"
      AllowedPublishers:
        SigningProfileVersionArns:
          - "arn:aws:signer:us-east-1:464622532012:/signing-profiles/DatadogLambdaSigningProfile/9vMI9ZAGLc"
      CodeSigningPolicies:
        UntrustedArtifactOnDeployment: Enforce

  CloudTrail:
    Condition: CreateCloudTrail
    Type: AWS::CloudTrail::Trail
    DependsOn:
      - S3Bucket
      - S3BucketPolicy
      - CloudFormationLifeCycle
    Properties:
      S3BucketName: !Ref S3Bucket
      IsLogging: true
      TrailName: !Ref TrailName
      EnableLogFileValidation: false
      IncludeGlobalServiceEvents: false
      IsMultiRegionTrail: false
      Tags:
        - Key: "env"
          Value: "dev"
      AdvancedEventSelectors:
        - Name: "Lambda Management Events"
          FieldSelectors:
            - Field: "eventCategory"
              Equals: ["Management"]
            - Field: "eventSource"
              NotEquals:
                - kms.amazonaws.com
                - rdsdata.amazonaws.com
            - Field: "readOnly"
              Equals:
                - "false"
  S3Bucket:
    Type: "AWS::S3::Bucket"
    DeletionPolicy: Delete
    Properties:
      BucketName: !If
        - HasBucketName
        - !Ref BucketName
        - !Join
          - "-"
          - - !Ref BucketPrefix
            - "bucket"
            - !Select
              - 0
              - !Split
                - "-"
                - !Select
                  - 2
                  - !Split
                    - /
                    - !Ref AWS::StackId

  S3BucketPolicy:
    Type: AWS::S3::BucketPolicy
    DependsOn:
      - S3Bucket
    Properties:
      Bucket: !Ref S3Bucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AWSCloudTrailAclCheck20150319
            Effect: Allow
            Principal:
              Service: cloudtrail.amazonaws.com
            Action: s3:GetBucketAcl
            Resource: !GetAtt S3Bucket.Arn
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub "arn:aws:cloudtrail:${AWS::Region}:${AWS::AccountId}:trail/${TrailName}"
          - Sid: AWSCloudTrailWrite20150319
            Effect: Allow
            Principal:
              Service: cloudtrail.amazonaws.com
            Action: s3:PutObject
            Resource: !Join ["/", [!GetAtt S3Bucket.Arn, "*"]]
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub "arn:aws:cloudtrail:${AWS::Region}:${AWS::AccountId}:trail/${TrailName}"
                s3:x-amz-acl: bucket-owner-full-control

  LambdaEventRule:
    Type: AWS::Events::Rule
    Properties:
      Description: "EventRule for piping lambda management events to Datadog lambda remote instrumenter"
      EventPattern:
        source:
          - aws.lambda
        detail:
          eventSource:
            - lambda.amazonaws.com
      State: "ENABLED"
      Targets:
        - Arn: !GetAtt LambdaFunction.Arn
          Id: "DatadogInstrumentingLambdaTarget"

  LambdaEventRulePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt LambdaFunction.Arn
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt LambdaEventRule.Arn

  LambdaSchedulerRole:
    Type: "AWS::IAM::Role"
    Properties:
      Path: "/"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: scheduler.amazonaws.com
            Action: "sts:AssumeRole"
      Policies:
        - PolicyName: root
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - lambda:InvokeFunction
                Resource: !GetAtt LambdaFunction.Arn

  LambdaInvocationScheduler:
    Type: AWS::Scheduler::Schedule
    Properties:
      Description: "Schedule for invoking the instrumenter lambda function"
      FlexibleTimeWindow:
        Mode: "OFF"
      ScheduleExpression: rate(5 minutes)
      State: ENABLED
      Target:
        Arn: !GetAtt LambdaFunction.Arn
        RoleArn: !GetAtt LambdaSchedulerRole.Arn
        Input: |
          {
              "event-type": "Scheduled Instrumenter Invocation"
          }
